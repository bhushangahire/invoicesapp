<%= form_for(@client) do |f| %>
	
	<% if @client.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@client.errors.count, "error") %> prohibited this invoice from being saved:</h2>
	
	      <ul>
	      <% @client.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>
	  
	<div class=" col-md-12 field-client-name"><%= f.text_field :name, placeholder: "Client Name", class: "form-control" %></div>
	
	<div class="form-group">
		<%= f.label :email %>
		<%= f.text_field :email, class: "form-control" %>
	</div>
	
	<div class="form-group">
		<%= f.label :address, "Client Address" %>
		<%= f.text_area :address, class: "form-control" %>
	</div>
	
	<div class="form-group">
		<%= f.submit "Register Client", class: "btn btn-large btn-primary" %>
	</div>

<% end %>
